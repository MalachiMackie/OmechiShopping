import { SortShoppingItemsPipe } from './sort-shopping-items.pipe';

describe('SortShoppingItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortShoppingItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
